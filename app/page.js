'use client';

import ChatContainer from '../components/ChatContainer';

export default function Home() {
  return (
    <main style={{ height: '100vh', overflow: 'hidden', backgroundColor: '#f0f2f5' }}>
      <ChatContainer />
    </main>
  );
}
